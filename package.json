{
  "name": "law-erp",
  "private": true,
  "workspaces": [
    "apps/web"
  ],
  "scripts": {
    "dev": "make dev",
    "dev:web": "npm run dev --workspace @law-erp/web",
    "dev:api": "make dev-api",
    "build": "npm run build --workspace @law-erp/web",
    "install:web": "npm install --workspace @law-erp/web"
  }
}
