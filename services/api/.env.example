# Database URL (API migrations and future persistence)
DATABASE_URL=postgres://law_erp:law_erp@localhost:5432/law_erp?sslmode=disable

# Optional separate training DB in production (same schema as training.*)
# TRAINING_DATABASE_URL=postgres://...
